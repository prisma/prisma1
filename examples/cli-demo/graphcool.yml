# GraphQL types
types: ./types.graphql

# functions
functions:
  like:
    handler:
      code:
        src: ./src/like.js
    type: subscription
    query: ./src/like.graphql

# modules
modules:
  github: modules/github/graphcool.yml

# permissions
permissions:
  - operation: '*'

# Permanent Auth Token / Root Tokens
rootTokens:
- like
