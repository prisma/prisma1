# import PostSubscriptionPayload from "./generated-schema.graphql"

type Query {
  posts: [Post!]!
}

type Mutation {
  createPost(data: PostCreateInput!): Post
}

type Subscription {
  posts: PostSubscriptionPayload
}
