# import PostSubscriptionPayload from "../generated/prisma.graphql"

type Query {
  posts: [Post!]!
}

type Mutation {
  createPost(data: PostCreateInput!): Post
}

type Subscription {
  publications: PostSubscriptionPayload
}
