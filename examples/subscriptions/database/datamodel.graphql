type Post {
  comments: [Comment!]! @relation(name: "CommentOnPost")
  id: ID! @unique
  isPublished: Boolean!
  title: String!
  text: String!
}

type Comment {
  body: String!
  id: ID! @unique
  post: Post! @relation(name: "CommentOnPost")
}
