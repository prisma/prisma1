type TypeWithId {
  id: ID! @id
  field: String!
}

type TypeWithoutId {
  id: ID! @id
  field: String!
}

type A {
  id: ID! @id
  x: TypeWithId! @relation(link: TABLE)
  y: TypeWithoutId! @relation(link: TABLE)
}

type B {
  id: ID! @id
  x: TypeWithId @relation(link: TABLE)
  y: TypeWithoutId @relation(link: TABLE)
}

type C {
  id: ID! @id
  x: [TypeWithId]
  y: [TypeWithoutId]
}

type D {
  id: ID! @id
  x: TypeWithId! @relation(link: TABLE)
  y: TypeWithoutId! @relation(link: TABLE)
}

type E {
  id: ID! @id
  x: TypeWithId @relation(link: TABLE)
  y: TypeWithoutId @relation(link: TABLE)
}

type F {
  id: ID! @id
  x: [TypeWithId]
  y: [TypeWithoutId]
}
