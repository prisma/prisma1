type Parent {
  id: ID! @unique
  p: String @unique
  children: [Child!]!
  j: [Join!]!
}

type Child @embedded {
  c: String @unique
  friendsOpt: [Friend!]! @mongoRelation(field: "friends")
}

type Friend {
  f: String @unique
  test: String
}

type Join {
  id: ID! @unique
  j: String
}
