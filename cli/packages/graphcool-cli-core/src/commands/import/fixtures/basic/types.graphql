type Post @model {
  id: ID! @isUnique
  title: String!
  description: String
  comments: [Comment!]! @relation(name: "PostComment")
}

type Comment @model {
  id: ID! @isUnique
  text: String!
  post: Post @relation(name: "PostComment")
}
