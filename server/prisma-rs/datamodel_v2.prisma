datasource database {
    provider = "sqlite"
    url      = "file:migration_engine.db"
}

generator photon {
    provider = "photonjs"
}


model User {
    id    String @id @default(cuid())
    email String @unique
    name  String
}