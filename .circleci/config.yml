jobs:
  build:
    docker:
      - image: circleci/node:8
    #working_directory: ~/project/cli
    steps:
      - checkout
      - run: ls $HOME
      - run: ls -la $HOME/project
      - run: yarn
      - run: yarn test
